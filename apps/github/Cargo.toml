[package]
name = "github"
version.workspace = true
edition.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
common = { path = "../common" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
